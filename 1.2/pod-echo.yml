apiVersion: v1
kind: Pod
metadata:
  name: netology-pod
spec:
  containers:
    - name: netology-pod
      image: gcr.io/kubernetes-e2e-test-images/echoserver:2.2
      ports:
        - containerPort: 8080